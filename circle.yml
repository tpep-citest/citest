## Customize the test machine
machine:

  timezone:
    America/Los_Angeles # Set the timezeone

## Customize test commands
test:
  override:
    - ls -lah
    - pwd
    - sudo apt-get install php5-mcrypt
    - php artisan test:core # use PHPunit for testing