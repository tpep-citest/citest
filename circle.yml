## Customize the test machine
machine:

  timezone:
    America/Los_Angeles # Set the timezeone

## Customize test commands
test:
  override:
    - ls -lah
    - pwd
    - phpunit tests/lib/util.php # use PHPunit for testing