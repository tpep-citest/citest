## Customize the test machine
machine:

  timezone:
    America/Los_Angeles # Set the timezeone

## Customize test commands
test:
  override:
    - phpunit --configuration tests/phpunit.xml # use PHPunit for testing